export enum CallType {
  AUDIO = 'audio',
  VIDEO = 'video'
}

export enum CallStatus {
  INITIATED = 'initiated',
  ONGOING = 'ongoing',
  ENDED = 'ended',
  MISSED = 'missed',
  REJECTED = 'rejected'
}
